## oleksii.xyz > src > 🎨 **styles**

files in this folder are mainly `.css` files which are inlined by `src/content` into pages.

- `.skeleton` gives a hint that file contain basic layout for yet not loaded page
